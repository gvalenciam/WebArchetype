import React, { FC } from "react";

interface FontsPageProps {}

export const FontsPage: FC<FontsPageProps> = ({}) => {
  return (
    <div>
      <p className="font-Roboto font-bold text-3xl">Roboto</p>
    </div>
  );
};

export default FontsPage;
